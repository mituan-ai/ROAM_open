# 方法配置：纯 GRU 回归器结构与训练超参数。
name: gru
family: sequence_regression
backend: torch
model:
  type: gru
  hidden_size: 8
  num_layers: 1
  dropout: 0.0
training:
  batch_size: 32
  max_epochs: 300
  learning_rate: 0.001
  weight_decay: 0.0
  device: auto
  random_seed: 42
  gradient_clip_norm: 1.0
